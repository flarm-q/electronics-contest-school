# 仓库说明

## 项目结构与模块划分
这是一个基于 Keil 的 STM32F103 平衡小车工程。`CORE/` 存放启动文件和 CMSIS 内核相关代码。`USER/` 存放应用入口和 Keil 工程文件，包括 `USER/main.c` 与 `USER/UpStanding_Car.uvprojx`。`SYSTEM/` 是基础公共模块，例如 `delay`、`sys`、`usart`。`HARDWARE/` 按功能拆分外设与业务模块，例如 `CONTROL/`、`TRACKING/`、`MPU6050/`、`MOTOR/`、`ENCODER/`。`STM32F10x_FWLib/` 保存 STM32 标准外设库源码。`OBJ/` 是编译产物目录，不应视为源代码。补充说明和图片资料位于 `README.md`、`项目分析报告.md`、`photo/`。

## 构建、测试与开发命令
使用 Keil 打开 `USER/UpStanding_Car.uvprojx` 进行编译。默认输出文件位于 `OBJ/`，主要包括 `UpStanding_Car.axf` 和 `UpStanding_Car.hex`。

- `UV4.exe -b USER\\UpStanding_Car.uvprojx -t "UpStanding_Car"`：命令行批量编译工程。
- `UV4.exe -f USER\\UpStanding_Car.uvprojx`：在 Keil IDE 中打开工程。
- `.\清除临时文件.bat`：清理临时文件，适合打包或整理工程前执行。

如果 `UV4.exe` 没有加入 `PATH`，就直接在 Keil IDE 里完成构建。

## 代码风格与命名约定
项目使用 C 语言，沿用现有代码风格：4 空格缩进，K&R 风格大括号。模块文件保持 `name.c` 与 `name.h` 成对出现。对外接口建议延续当前模块前缀命名方式，例如 `Tracking_SetSpeed()`、`Motor_Init()`。文件内部状态优先使用 `static`，涉及硬件的全局变量应尽量少且语义明确。

新增代码建议统一命名语义，避免含义模糊的缩写。变量和局部函数优先使用清晰的小写风格；常量、宏使用全大写下划线风格，例如 `MAX_BUFFER_SIZE`、`I2C_TIMEOUT_MS`；结构体、枚举类型名使用驼峰式或与现有模块保持一致。若项目内新增一整套模块，应在该模块内部保持命名一致，不要混用多套规则。

注释以解释“为什么这样做”为主，不要重复代码表面含义。函数注释建议写在函数上方，至少说明功能、关键参数、返回值和硬件或中断上下文限制。可以保留必要的中文注释，但新增注释应简短、聚焦技术含义。

## 模块职责与分层约束
`USER/main.c` 只保留主函数、初始化流程和主循环调度，不要继续在其中堆叠具体业务函数实现。新增功能应按外设或业务拆分到独立的 `.c/.h` 模块中，降低耦合，便于后续维护。

建议按嵌入式常见分层组织代码：应用逻辑放在上层调度模块，板级外设封装放在 `SYSTEM/` 或对应硬件模块，芯片底层驱动继续依赖 `STM32F10x_FWLib/`。跨层调用保持单向依赖，上层调用下层，避免反向依赖和循环包含。

中断服务函数中只做最小工作，例如置标志、搬运数据、记录时间戳；较重的业务处理尽量下沉到主循环或控制任务中。公共头文件只暴露必要接口，减少无关的全局变量和头文件传染式依赖。

## 测试说明
当前仓库没有独立的单元测试框架，主要依赖编译验证和实机验证。修改完成后，先确认工程在 Keil 中可以正常编译，且不要引入新的告警；再在开发板上验证相关行为，例如平衡控制、编码器方向、OLED 显示、巡线响应等。若连续进行了多处代码修改，应至少做一次完整编译检查，避免只改不编。若改动影响功能逻辑，提交时应注明实际做过的板级测试。

## 提交与合并请求规范
现有提交信息以中文短句为主，强调功能点，例如 `数字量巡线优化及在线调参`、`方向问题解决`。后续提交建议保持同样风格，用一行概括本次改动的核心目的。提交合并请求时，建议包含改动摘要、涉及模块、硬件验证结果；如果改动影响显示、接线或交互，也应附上照片或截图。除非明确需要提交发布产物，否则不要把 `OBJ/` 下的编译输出纳入版本管理。
