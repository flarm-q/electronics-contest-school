# STM32 平衡小车工程分析报告

## 1. 工程概况

这是一个基于 `STM32F103C8` 的两轮自平衡小车工程，使用 STM32 标准外设库开发，Keil 工程文件为 `USER/UpStanding_Car.uvprojx`。

工程实现的核心能力：

- MPU6050 姿态采集与 DMP 解算
- 两路编码器测速
- 两路电机 PWM 驱动与方向控制
- 三环闭环控制：直立环 + 速度环 + 转向环
- USART3 串口输入高层目标
- 超声波测距与简单避障
- OLED 实时显示角度、距离、速度
- 八路巡线串口接入
- K230 颜色识别视觉避障

从控制对象上看，这个系统本质上是一个“倒立摆”控制系统，小车要通过不断驱动车轮，让车体始终维持在平衡点附近。

---

## 2. 实际工程结构

```text
STM32平衡小车（520电机）
├─ CORE/                  Cortex-M3 内核与启动文件
├─ STM32F10x_FWLib/       STM32 标准外设库
├─ SYSTEM/                系统级基础模块
│  ├─ delay/              延时
│  ├─ sys/                全局头文件、位带 GPIO 宏、NVIC 配置
│  └─ usart/              USART1 调试串口
├─ HARDWARE/              业务硬件驱动
│  ├─ CONTROL/            平衡控制算法
│  ├─ ENCODER/            编码器测速
│  ├─ EXTI/               MPU6050 外部中断输入
│  ├─ MOTOR/              电机方向控制
│  ├─ MPU6050/            MPU6050 驱动 + DMP
│  ├─ OLED/               OLED 显示
│  ├─ PWM/                电机 PWM 输出
│  ├─ SENSOR/             超声波模块源码（目录存在，但未加入 Keil 编译）
│  └─ USART3/             蓝牙串口
├─ USER/                  应用层
│  ├─ main.c              主程序入口
│  ├─ stm32f10x_it.c      异常模板
│  └─ UpStanding_Car.uvprojx
└─ OBJ/                   编译产物
```

### 结构上的一个关键点

虽然目录中有 `HARDWARE/SENSOR/sensor.c`，但它没有被 `UpStanding_Car.uvprojx` 加入编译。

当前实际生效的超声波逻辑，是直接写在 `USER/main.c` 里的：

- `TIM3_Int_Init()`
- `Senor_Using()`
- `TRIG/ECHO` 宏

所以从“实际运行架构”看，超声波测距并没有完成模块化，仍然是主程序内嵌实现。

---

## 3. 系统启动与运行流程

`main()` 的初始化顺序如下：

1. `delay_init()`：初始化延时基准
2. `NVIC_Config()`：配置中断优先级，主要打开 `EXTI9_5`、`USART2` 和 `USART3`
3. `uart1_init(115200)`：调试串口
4. `uart3_init(9600)`：初始化 USART3，当前用于接收 K230 视觉结果
5. `Tracking_Usart2_Init(115200)`：初始化八路巡线模块串口
6. `OLED_Init()`：初始化显示
7. `MPU_Init()` + `mpu_dmp_init()`：初始化 MPU6050 与 DMP
8. `MPU6050_EXTI_Init()`：配置 MPU6050 的外部中断输入
9. `Encoder_TIM2_Init()`：左轮编码器
10. `Encoder_TIM4_Init()`：右轮编码器
11. `Motor_Init()`：电机方向引脚
12. `PWM_Init_TIM1(0, 7199)`：TIM1 输出 PWM
13. `Tracking_SendControlData(0,0,1)`：请求八路巡线模块输出数字量数据

之后进入 `while(1)`，主循环只做两类低速任务：

- OLED 显示角度、巡线偏差、速度
- USART2 中断后台持续更新八路巡线状态

真正的平衡控制不在 `while(1)` 中执行，而是在 `EXTI9_5_IRQHandler()` 中断服务函数中执行。

这说明工程采用的是：

- 主循环负责显示
- USART2 中断负责巡线数据接收
- USART3 中断负责接收 K230 颜色识别结果
- 外部中断负责高实时控制

这是典型的“前后台”嵌入式结构。

---

## 4. 项目实现的功能

### 4.1 自平衡

通过 MPU6050 获取车体姿态角和角速度，通过编码器获取轮速，再通过 PID 控制输出 PWM，使车体维持直立。

### 4.2 前进 / 后退

当前代码中，前后目标速度主要有 3 个来源：

- K230 视觉避障：直接给出减速后的 `Target_Speed`
- 八路巡线：`Tracking_SetSpeed()` 给出固定前进速度
- 原手动控制兜底逻辑：仍保留 `Fore/Back` 变量兼容入口，但 USART3 已不再按蓝牙单字节协议更新它们

速度环再把速度误差转换为俯仰角补偿，让小车前进或后退。

### 4.3 左转 / 右转

当前代码中，转向目标同样分为 3 类来源：

- K230 视觉避障：根据目标在左 / 中 / 右决定绕行方向
- 八路巡线：由 `Tracking_TurnPD(gyroz)` 根据巡线偏差计算
- 原左右转向变量兜底：`Left/Right` 变量仍保留，但 USART3 已不再作为蓝牙单字节遥控入口

### 4.4 八路巡线

通过 USART2 接收八路红外巡线模块的数字量数据，解析 `x1~x8` 后计算当前偏差。

巡线模式下：

- `Tracking_SetSpeed()` 给定固定前进速度
- `Tracking_TurnPD(gyroz)` 根据红外偏差和 Z 轴陀螺仪输出转向量
- 原直立环、速度环和电机输出链路保持不变

原超声波测距已关闭，释放 `PA2/PA3` 给 USART2 使用。

### 4.5 OLED 状态显示

OLED 实时显示：

- `Pitch` 角度
- 巡线偏差
- 轮速估算值

### 4.6 倾倒保护

当车体倾角相对机械中值偏差超过 `60°` 时，`Stop()` 会把 PWM 清零并置停机标志，防止电机继续暴力输出。

### 4.7 K230 视觉避障

当前工程已经接入 `HARDWARE/K230/color_ecognition.py` 对应的上位视觉方案。

K230 通过串口发送颜色识别结果帧：

```text
$C,color_id,pos,size#
```

示例：

```text
$C,1,L,356#
```

含义：

- `color_id`：颜色编号，`1=红`，`2=绿`，`3=蓝`
- `pos`：目标横向位置，`L=左`，`C=中`，`R=右`
- `size`：色块面积

STM32 在 `USART3_IRQHandler()` 中解析该数据，并在控制中断中按优先级接管高层速度与转向目标，实现简化视觉避障。

---

## 5. 控制架构与原理

### 5.1 控制总体思路

平衡车不能直接“命令它站稳”，而是要通过控制轮子运动，让轮子始终追着重心跑。

控制链条是：

1. 先知道车身现在往哪边倒
2. 再算出轮子该向哪边加速
3. 通过电机驱动车轮移动
4. 利用车轮加速度把车体重新拉回平衡点

这个工程采用了经典的三环控制：

- 直立环：控制“不倒”
- 速度环：控制“走多快”
- 转向环：控制“向哪转”

其中速度环的输出不是直接给电机，而是作为直立环的目标角补偿，这正是平衡车控制的核心思路。

---

## 6. 三环控制详细分析

### 6.1 直立环 PD

函数：

```c
int Vertical(float Med,float Angle,float gyro_Y)
{
    PWM_out = Vertical_Kp*(Angle-Med) + Vertical_Kd*(gyro_Y-0);
    return PWM_out;
}
```

参数：

- `Vertical_Kp = -400`
- `Vertical_Kd = -1.92`

含义：

- `Angle - Med`：当前车体偏离机械中值的角度误差
- `gyro_Y`：俯仰角速度

原理：

- `Kp` 负责把车体拉回平衡点
- `Kd` 负责抑制摆动，提供阻尼

这就是一个典型的倒立摆 PD 控制器。

### 6.2 速度环 PI

函数：

```c
Encoder_Err = (encoder_left + encoder_right) - Target;
EnC_Err_Lowout = (1-a)*Encoder_Err + a*EnC_Err_Lowout_last;
Encoder_S += EnC_Err_Lowout;
PWM_out = Velocity_Kp*EnC_Err_Lowout + Velocity_Ki*Encoder_S;
```

参数：

- `Velocity_Kp = -0.44`
- `Velocity_Ki = -0.0022`
- 低通滤波系数 `a = 0.7`

原理：

- 编码器和得到当前速度
- 与目标速度作差，得到速度误差
- 先低通滤波，减小测速抖动
- 再积分，消除长期偏差
- 输出结果作为“角度补偿”

也就是说：

- 想让车更快前进，就不是直接把 PWM 拉高
- 而是让车“故意略微前倾”
- 车为了防止摔倒，自然就会往前跑

这是平衡车速度控制的本质。

### 6.3 转向环

函数：

```c
int Turn(int gyro_Z,int RC)
{
    PWM_out = Turn_Kd*gyro_Z + Turn_Kp*RC;
    return PWM_out;
}
```

参数：

- `Turn_Kp = 20`
- `Turn_Kd = 0.6`

原理：

- `Turn_Kp * RC`：根据遥控指令产生转向目标
- `Turn_Kd * gyro_Z`：对偏航角速度做约束

当没有左右转向命令时，程序保留 `Turn_Kd=0.6`，用于抑制无意偏航。

当收到左转或右转命令时，程序把 `Turn_Kd` 设为 `0`，让车更容易执行主动转向。

### 6.4 三环合成方式

在 `EXTI9_5_IRQHandler()` 中：

```c
Velocity_out = Velocity(Target_Speed, Encoder_Left, Encoder_Right);
Vertical_out = Vertical(Velocity_out + Med_Angle, Pitch, gyroy);
Turn_out = Turn(gyroz, Turn_Speed);

PWM_out = Vertical_out;
MOTO1 = PWM_out - Turn_out;
MOTO2 = PWM_out + Turn_out;
```

可以看出：

- 速度环先输出一个“目标倾角补偿”
- 直立环产生基础驱动力
- 转向环在左右轮上做差分叠加

最终得到：

- 左轮 = 平衡输出 - 转向输出
- 右轮 = 平衡输出 + 转向输出

这就是标准差速平衡车控制方式。

---

## 7. 中断驱动关系

### 7.1 控制中断源

`PB5` 被配置为 `EXTI_Line5` 下降沿触发。

这一路来自 MPU6050 的中断输出脚，表示 DMP 有新姿态数据可读。

因此控制节拍是：

1. MPU6050 产生新数据
2. INT 拉低
3. `EXTI9_5_IRQHandler()` 进入
4. 读取姿态和编码器
5. 运行三环控制
6. 更新电机输出

这比在 `while(1)` 轮询更稳定，实时性更高。

### 7.2 USART3 串口中断

当前 `USART3_IRQHandler()` 已不再实现原来的蓝牙单字节方向遥控，而是改为解析 K230 发送的颜色识别结果帧。

当前协议格式：

```text
$C,color_id,pos,size#
```

例如：

```text
$C,1,L,356#
```

中断处理流程为：

1. 遇到 `$` 认为一帧开始
2. 连续缓存后续字节
3. 遇到 `#` 认为一帧结束
4. 解析出 `color_id / pos / size`
5. 更新全局 `K230_ColorFrame_t`
6. 重装视觉保活计数器

说明：

- `Fore / Back / Left / Right` 变量仍然保留，用于兼容旧控制代码中的 `extern`
- 但收到 K230 新帧时会被强制清零，避免旧手动逻辑残留干扰

### 7.3 编码器定时器

TIM2 和 TIM4 都被配置成编码器接口模式，定时器计数值就是固定时间窗口内的脉冲增量。

程序每次进入控制中断时调用 `Read_Speed()`：

- 读计数器
- 立刻清零

因此得到的是“一个控制周期内的轮速增量”。

---

## 8. 引脚关系总表

### 8.1 核心控制与传感器引脚

| 模块 | 引脚 | 作用 |
|---|---|---|
| 左编码器 TIM2 | `PA0`, `PA1` | TIM2 编码器接口输入 |
| 右编码器 TIM4 | `PB6`, `PB7` | TIM4 编码器接口输入 |
| MPU6050 软件 I2C | `PB4` SCL, `PB3` SDA | 姿态传感器通信 |
| MPU6050 中断 | `PB5` | DMP 数据就绪中断输入 |
| 八路巡线 USART2 | `PA2` TX, `PA3` RX | 巡线模块串口通信 |
| 超声波 | 已关闭 | 原 `PA2/PA3` 已让给 USART2 |
| OLED 软件 I2C | `PB8` SCL, `PB9` SDA | OLED 显示 |
| USART3 / K230 | `PB10` TX, `PB11` RX | 当前用于接收 K230 颜色识别结果 |
| 调试 USART1 | `PA9` TX, `PA10` RX | 串口打印/调试 |

### 8.2 电机驱动引脚

| 功能 | 引脚 | 说明 |
|---|---|---|
| 左电机 PWM | `PA8` | TIM1_CH1 |
| 右电机 PWM | `PA11` | TIM1_CH4 |
| 左电机方向 Ain1 | `PB14` | H 桥方向控制 |
| 左电机方向 Ain2 | `PB15` | H 桥方向控制 |
| 右电机方向 Bin1 | `PB13` | H 桥方向控制 |
| 右电机方向 Bin2 | `PB12` | H 桥方向控制 |

### 8.3 一个需要注意的点

MPU6050 软件 I2C 使用了 `PB3/PB4`，OLED 又在初始化里关闭了 JTAG，只保留 SWD。

这是合理的，因为 `PB3/PB4` 默认和 JTAG 复用，不关闭 JTAG 时，这两根线可能无法正常作为普通 GPIO 使用。

---

## 9. 各模块职责分析

### 9.1 `SYSTEM/sys`

作用：

- 汇总工程头文件
- 提供 `PAout/PBin` 这一类位带操作宏
- 做 NVIC 中断优先级配置

这是整个工程的“公共底座”。

### 9.2 `HARDWARE/MPU6050`

作用：

- 软件 I2C 读写 MPU6050
- 初始化陀螺仪、加速度计
- 使用 DMP 直接解算欧拉角

这是整个平衡控制的数据源核心。

### 9.3 `HARDWARE/CONTROL`

作用：

- 承担主闭环算法
- 在中断里完成采样、控制计算、驱动输出

这是工程最关键的模块。

### 9.4 `HARDWARE/ENCODER`

作用：

- 用 TIM2/TIM4 编码器模式采集左右轮速度

左轮值在控制里被取了负号：

```c
Encoder_Left = -Read_Speed(2);
Encoder_Right = Read_Speed(4);
```

原因是左右电机/编码器机械安装方向相反，为了让“前进方向速度”在控制层保持统一符号。

### 9.5 `HARDWARE/MOTOR` 与 `HARDWARE/PWM`

作用：

- `MOTOR`：控制方向和安全停机
- `PWM`：输出占空比

两者组合后，形成完整的电机驱动层。

### 9.6 `HARDWARE/USART3`

作用：

- 接收 K230 颜色识别结果
- 解析 `$C,color_id,pos,size#` 协议帧
- 提供视觉结果保活与超时失效机制

也就是说，`USART3` 这个模块名虽然沿用了原命名，但当前职责已经从“蓝牙遥控输入”转成了“视觉结果输入”。

### 9.7 `HARDWARE/OLED`

作用：

- 软件 I2C 驱动 OLED
- 给调试和演示提供可视化界面

---

## 10. 关键参数

| 参数 | 数值 | 说明 |
|---|---:|---|
| 主控芯片 | `STM32F103C8` | 由 `uvprojx` 配置可见 |
| PWM 周期 | `ARR = 7199` | 配合 `PSC = 0` |
| PWM 频率 | 约 `10kHz` | `72MHz / 7200` |
| PWM 限幅 | `-7200 ~ 7200` | 对应满量程占空比 |
| 机械中值 | `Med_Angle = 0` | 需要按机械结构校准 |
| 直立环 Kp | `-430` | 角度误差比例 |
| 直立环 Kd | `-1.92` | 角速度阻尼 |
| 速度环 Kp | `-0.44` | 速度误差比例 |
| 速度环 Ki | `-0.0022` | 速度积分 |
| 转向环 Kp | `20` | 遥控转向量比例 |
| 转向环 Kd | `0.6` | 偏航角速度约束 |
| 最大前后目标速度 | `40` | `SPEED_Y` |
| 最大转向目标速度 | `100` | `SPEED_Z` |
| 倾倒保护阈值 | `60°` | 超过即停机 |
| USART3 波特率 | `9600` | 当前接 K230 |
| 调试串口波特率 | `115200` | USART1 |
| DMP 频率 | `100Hz` | `DEFAULT_MPU_HZ = 100` |
| K230 避障基础速度 | `8` | `K230_AVOID_BASE_SPEED` |
| K230 强转向量 | `45` | `K230_AVOID_TURN_STRONG` |
| K230 中等转向量 | `30` | `K230_AVOID_TURN_MID` |
| K230 最小有效面积 | `200` | `K230_AVOID_MIN_SIZE` |
| 视觉保活周期 | `40` 个控制周期 | `K230_FRAME_TIMEOUT_TICKS` |

---

## 11. 这个工程的优点

- 控制与显示分层基本清楚
- 三环控制结构合理，属于平衡车经典做法
- 姿态解算用 DMP，减轻了 MCU 运算压力
- 采用中断触发控制，实时性优于轮询
- 有基本安全保护和避障策略

---

## 12. 当前工程存在的不足

### 12.1 超声波模块存在“目录与实际编译不一致”

`HARDWARE/SENSOR` 有完整实现，但没有加入 Keil 工程；
`main.c` 又保留了一份同类代码。

这会带来两个问题：

- 目录结构看起来模块化，但实际不是
- 后续维护时容易误改错文件

### 12.2 主程序承担了不该承担的驱动逻辑

`main.c` 中直接实现超声波初始化与测距，职责过重。

更合理的方式是：

- `main.c` 只保留调度逻辑
- 外设实现全部放回各自模块

### 12.3 蓝牙控制逻辑较粗糙

当前蓝牙协议只靠单字节方向码，缺少：

- 校验
- 连续速度值
- 转向比例值
- 超时保护

适合演示，不太适合做更复杂控制。

### 12.4 超声波与平衡控制耦合较弱

现在超声波仅影响 `Target_Speed` 的增减趋势，没有做：

- 更稳定的距离闭环
- 滞回区间
- 障碍物状态机

所以它更像“简单防撞辅助”，不是完整避障系统。

### 12.5 文档与当前代码存在阶段差异

当前代码已经进入“巡线 + K230 视觉避障”阶段，但原报告前半部分仍保留了较多“蓝牙遥控 + 超声波主导”的表述。

更准确的当前实际状态应该是：

- 超声波代码仍在仓库中，但主流程未启用
- USART3 当前接的是 K230 视觉结果，不是原蓝牙单字节遥控
- 高层控制优先级已经变为：`K230视觉避障 > 八路巡线 > 旧手动逻辑兜底`

### 12.6 `tracking.c / tracking.h` 注释存在乱码

`HARDWARE/TRACKING/tracking.c` 和 `tracking.h` 的源码实现是正常可读和可工作的，但部分中文注释存在编码乱码。

这不会直接影响编译运行，但会带来两个维护问题：

- 新接手的人很难快速理解协议和逻辑
- 后续再修改巡线协议时，容易因为注释失真而误判行为

建议后续统一转成明确编码并重写注释说明。

---

## 13. 如果后续要继续优化，建议这样改

1. 把 `main.c` 中的超声波代码移回 `HARDWARE/SENSOR`，并加入 Keil 工程统一管理。
2. 给控制流程补一张状态机，例如“站立 / 前进 / 转向 / 倾倒保护 / 避障减速”。
3. 蓝牙协议改成带速度量和转向量的数据帧，而不是固定单字节命令。
4. 给超声波避障增加阈值滞回，避免 50cm 附近反复抖动。
5. 把 PID 参数、机械中值做成可在线调试变量，便于标定。
6. 增加电池电压检测，否则低电压时控制表现会明显变化。
7. 把 `USART3` 目录名或注释说明和实际功能统一，避免“文件名叫蓝牙，实际跑视觉”的认知偏差。
8. 整理 `tracking.c / tracking.h` 中文注释乱码问题，降低维护成本。
9. 如果还需要保留蓝牙遥控，建议给 K230 和遥控分别分配独立串口或统一设计多类型协议帧。

---

## 14. 总结

这个工程的核心不是“电机驱动”本身，而是一个典型的 `STM32 + MPU6050 + 编码器 + 三环控制` 的平衡车控制系统。

它的架构特征可以概括成一句话：

> 姿态中断驱动闭环控制，编码器提供速度反馈，蓝牙给目标指令，电机 PWM 完成执行，OLED 和超声波负责辅助交互。

如果从学习价值看，这个工程非常适合用来理解以下几件事：

- 平衡车为什么要用三环控制
- 为什么速度控制要通过“改目标倾角”实现
- 为什么姿态采样通常放在中断里
- 差速转向是怎样和直立控制叠加在一起的
- STM32 定时器如何同时用于 PWM、编码器和测距

---

## 15. 八路巡线功能移植说明

### 15.1 移植来源

八路巡线功能参考工程：

`E:\BaiduNetdiskDownload\八路巡线红外传感器\程序源码\2.小车巡线\3.STM32平衡车\BalancedCar_tracking_Eight_usart_hard\BalancedCar_tracking_Eight_usart_hard`

主要参考了以下逻辑：

- `APP/app_usart2.c`：八路巡线模块串口协议解析
- `APP/app_tracking.c`：八路巡线偏差判断和转向 PD 控制
- `BSP/bsp_usart2.c`：USART2 初始化方式

### 15.2 本工程新增文件

新增目录：

`HARDWARE/TRACKING/`

新增文件：

- `tracking.h`
- `tracking.c`

Keil 工程 `USER/UpStanding_Car.uvprojx` 已加入：

- 头文件路径：`..\HARDWARE\TRACKING`
- 编译文件：`..\HARDWARE\TRACKING\tracking.c`

### 15.3 引脚冲突处理

参考工程八路巡线模块通过 USART2 通信：

| 功能 | 引脚 |
|---|---|
| USART2_TX | `PA2` |
| USART2_RX | `PA3` |

当前平衡小车原工程中，`PA2/PA3` 原本被超声波测距占用：

| 原功能 | 引脚 |
|---|---|
| 超声波 ECHO | `PA2` |
| 超声波 TRIG | `PA3` |

本次按需求关闭超声波测距，因此 `main()` 中不再调用：

- `TIM3_Int_Init()`
- `Senor_Using()`

这样 `PA2/PA3` 专用于八路巡线 USART2，不再与超声波模块冲突。

### 15.4 串口协议

程序启动后会调用：

```c
Tracking_Usart2_Init(115200);
Tracking_SendControlData(0,0,1);
```

含义：

- USART2 波特率：`115200`
- 向巡线模块发送命令：只请求数字量数据
- 期望接收帧格式：`$D,x1:0,x2:0,x3:0,x4:0,x5:0,x6:0,x7:0,x8:0#`

解析结果保存到：

```c
Tracking_IR_Data[0] ~ Tracking_IR_Data[7]
```

其中：

- `Tracking_IR_Data[0]` 对应 `x1`
- `Tracking_IR_Data[7]` 对应 `x8`
- 按参考工程逻辑，`0` 表示该路检测到黑线，`1` 表示未检测到黑线

### 15.5 控制逻辑接入

巡线功能接入在 `HARDWARE/CONTROL/control.c` 的 `EXTI9_5_IRQHandler()` 中。

控制策略：

- 未收到有效巡线帧时：保持原蓝牙遥控逻辑
- 收到有效巡线帧后：进入巡线模式
- 巡线模式下：`Target_Speed` 由 `Tracking_SetSpeed()` 固定给定
- 巡线模式下：转向输出由 `Tracking_TurnPD(gyroz)` 计算

最终仍然沿用原工程的差速合成方式：

```c
MOTO1 = PWM_out - Turn_out;
MOTO2 = PWM_out + Turn_out;
```

所以巡线功能只替换“目标速度与转向量来源”，不改动直立环、速度环、电机驱动、编码器和 MPU6050 姿态采样。

### 15.6 巡线偏差定义

`Tracking_GetError()` 根据 `x1~x8` 状态计算偏差：

- 负值：线偏左，需要向左侧修正
- 正值：线偏右，需要向右侧修正
- `0`：居中或特殊直行状态

转向控制参数在 `tracking.c` 中：

```c
#define TRACKING_SPEED 15
#define TRACKING_TURN_KP 270
#define TRACKING_TURN_KI 0.01f
#define TRACKING_TURN_KD 0.15f
```

如果实车巡线方向反了，优先把 `TRACKING_TURN_KP` 改成负值，而不是改电机接线。

### 15.7 OLED 显示变化

原来 OLED 第 4 行显示超声波距离 `length`。

现在改为显示巡线偏差：

```c
OLED_Num3(8,4,Tracking_GetError());
```

这样可以直接观察巡线识别是否正常：

- 传感器居中压线时应接近 `0`
- 左侧传感器压线时偏差应为负
- 右侧传感器压线时偏差应为正

### 15.8 当前保留与关闭的功能

保留：

- 自平衡
- 编码器速度环
- MPU6050 姿态采样
- 蓝牙串口 USART3
- OLED 显示
- 倾倒保护

关闭：

- 超声波测距运行逻辑
- 超声波对前进速度的距离判断

说明：

`main.c` 中原超声波函数代码仍保留，但没有再调用。这样做的目的是减少无关改动，后续如果要恢复超声波，需要重新分配不冲突的引脚，不能再使用 `PA2/PA3`。

---

## 16. 工程 IO 使用汇总（按当前代码）

本节基于当前工程源码实际初始化代码（`main.c` 调用链 + `HARDWARE/*`、`SYSTEM/*`）重新整理，优先以“当前主流程实际启用”作为判断标准，而不是只看目录中是否存在某个模块源码。

### 16.1 GPIO/外设引脚映射

| 引脚 | 模式 | 对应外设/功能 | 代码位置 |
|---|---|---|---|
| PA0 | 浮空输入 | TIM2_CH1 编码器输入 | `HARDWARE/ENCODER/encoder.c` |
| PA1 | 浮空输入 | TIM2_CH2 编码器输入 | `HARDWARE/ENCODER/encoder.c` |
| PA2 | 复用推挽输出 | USART2_TX，八路巡线模块配置/发送 | `HARDWARE/TRACKING/tracking.c` |
| PA3 | 浮空输入 | USART2_RX，八路巡线模块数据接收 | `HARDWARE/TRACKING/tracking.c` |
| PB6 | 浮空输入 | TIM4_CH1 编码器输入 | `HARDWARE/ENCODER/encoder.c` |
| PB7 | 浮空输入 | TIM4_CH2 编码器输入 | `HARDWARE/ENCODER/encoder.c` |
| PA8 | 复用推挽输出 | TIM1_CH1 PWM（电机A PWM） | `HARDWARE/PWM/pwm.c` |
| PA9 | 复用推挽输出 | USART1_TX（调试串口） | `SYSTEM/usart/usart.c` |
| PA10 | 浮空输入 | USART1_RX（调试串口） | `SYSTEM/usart/usart.c` |
| PA11 | 复用推挽输出 | TIM1_CH4 PWM（电机B PWM） | `HARDWARE/PWM/pwm.c` |
| PB3 | 软件 IIC 数据线 | MPU6050 SDA，位带双向口 | `HARDWARE/MPU6050/mpuiic.c` |
| PB4 | 软件 IIC 时钟线 | MPU6050 SCL | `HARDWARE/MPU6050/mpuiic.c` |
| PB5 | 上拉输入 + EXTI5 | MPU6050 DMP 数据就绪中断输入 | `HARDWARE/EXTI/exti.c` |
| PB12 | 推挽输出 | 电机方向 Bin2 | `HARDWARE/MOTOR/motor.c`, `HARDWARE/MOTOR/motor.h` |
| PB13 | 推挽输出 | 电机方向 Bin1 | `HARDWARE/MOTOR/motor.c`, `HARDWARE/MOTOR/motor.h` |
| PB14 | 推挽输出 | 电机方向 Ain1 | `HARDWARE/MOTOR/motor.c`, `HARDWARE/MOTOR/motor.h` |
| PB15 | 推挽输出 | 电机方向 Ain2 | `HARDWARE/MOTOR/motor.c`, `HARDWARE/MOTOR/motor.h` |
| PB8 | 推挽输出 | OLED 软件 IIC SCL | `HARDWARE/OLED/oled.c`, `oled.h` |
| PB9 | 推挽输出 | OLED 软件 IIC SDA | `HARDWARE/OLED/oled.c`, `oled.h` |
| PB10 | 复用推挽输出 | USART3_TX，当前主要保留给 K230 串口联调/回发 | `HARDWARE/USART3/usart3.c` |
| PB11 | 浮空输入 | USART3_RX，当前接收 K230 颜色识别结果 | `HARDWARE/USART3/usart3.c` |

补充说明：

- `PA2/PA3` 在当前主流程中已经明确分配给 `USART2` 巡线模块。
- `PB10/PB11` 在当前主流程中已经明确分配给 `USART3`，且 `USART3` 当前承载的是 K230 视觉结果输入，不再按原蓝牙单字节协议工作。
- `PB3/PB4` 用作 MPU6050 软件 IIC，因此工程中通过关闭 JTAG、保留 SWD 的方式释放相关复用引脚。

### 16.2 中断线与定时器通道

- `EXTI_Line5`：来源 `PB5`，下降沿触发，对应 MPU6050 DMP 数据就绪中断。
- `TIM1`：PWM 输出使用 `CH1(PA8)`、`CH4(PA11)`，驱动左右电机占空比。
- `TIM2`：编码器接口模式（TI12），输入 `PA0/PA1`，用于左轮测速。
- `TIM4`：编码器接口模式（TI12），输入 `PB6/PB7`，用于右轮测速。
- `USART2`：启用 `RXNE` 接收中断，用于八路巡线模块串口接收。
- `USART3`：启用 `RXNE` 接收中断，用于 K230 颜色识别结果串口接收。
- `USART1`：调试串口已初始化，但在 `NVIC_Config()` 中其中断配置被注释，当前主要按普通串口使用。

需要特别注意的是，当前闭环控制的真正节拍源不是定时器，而是 `PB5 -> EXTI9_5_IRQHandler()` 这一路姿态中断。

### 16.3 已识别的 IO 复用/冲突关系

- `PA2/PA3`：
  当前用于 `USART2` 巡线通信；而 `USER/main.c` 与 `HARDWARE/SENSOR/sensor.c` 中保留的超声波方案同样占用 `PA2/PA3`，两者按当前接线不能同时使用。
- `PB3/PB4`：
  当前用于 MPU6050 软件 IIC；这两根线与 JTAG 复用，因此工程依赖关闭 JTAG 保留 SWD 的配置来释放 GPIO 功能。
- `PB10/PB11`：
  当前用于 `USART3 <-> K230` 串口通信；如果后续要恢复传统蓝牙模块，就必须重新明确 USART3 的协议归属，或者改接到别的串口。

因此，当前工程中最关键的 IO 冲突不是“代码能不能编译”，而是：

- 超声波测距 与 巡线串口 争用 `PA2/PA3`
- K230 视觉输入 与 旧蓝牙遥控 在逻辑上争用 `USART3`

### 16.4 未在当前主流程中实际启用的接口

- 超声波测距：
  仓库中存在 `HARDWARE/SENSOR/sensor.c`，`USER/main.c` 中也保留了 `TIM3_Int_Init()` 与 `Senor_Using()`，但当前 `main()` 并未调用它们，因此这套 IO 方案处于“代码保留、主流程未启用”状态。
- TIM3：
  当前没有在主流程中作为超声波测距定时器投入运行，相关中断逻辑也未启用。
- ADC：
  未发现有效 ADC 初始化与采样调用进入 `main` 主流程。
- 硬件 I2C / SPI：
  当前工程主要使用软件模拟 IIC（MPU6050 与 OLED），未看到硬件 I2C/SPI 在主流程中启用。

### 16.5 外接模块连线视角汇总

从“板外连接关系”看，当前实际有效连接可以概括为：

| 外接模块 | STM32 侧引脚 | 当前状态 |
|---|---|---|
| MPU6050 | `PB3/PB4/PB5` | 已启用 |
| 左编码器 | `PA0/PA1` | 已启用 |
| 右编码器 | `PB6/PB7` | 已启用 |
| 电机驱动 PWM | `PA8/PA11` | 已启用 |
| 电机方向控制 | `PB12~PB15` | 已启用 |
| OLED | `PB8/PB9` | 已启用 |
| 巡线模块 | `PA2/PA3` | 已启用 |
| K230 串口输出到 STM32 | `PB11` 为主要接收脚，`PB10` 预留发送 | 已启用 |
| 调试串口 | `PA9/PA10` | 已启用 |
| 超声波模块 | 原设计 `PA2/PA3` | 当前主流程关闭 |

---

## 17. 当前代码增量更新（基于 2026-05-17 实际源码）

本节用于补充原报告中尚未覆盖，或已经与当前代码不一致的部分。

### 17.1 当前高层行为优先级

`HARDWARE/CONTROL/control.c` 中，控制优先级已经明确写成：

1. `K230_AvoidanceActive()` 生效时，视觉避障优先
2. 否则如果 `Tracking_IsActive()` 生效，则进入巡线模式
3. 否则才回退到原始 `Fore/Back/Left/Right` 兜底逻辑

这意味着当前工程已经不是“蓝牙主控、巡线为辅”的结构，而是“视觉 / 巡线主导，旧手动逻辑保底”的结构。

### 17.2 K230 视觉接入的控制方式

K230 识别脚本位于：

`HARDWARE/K230/color_ecognition.py`

其主要行为是：

- 在 K230 端识别红、绿、蓝三类色块
- 只选择当前画面中面积最大的一个色块
- 按 `640` 宽度把目标划分为左 / 中 / 右三类位置
- 通过 UART1 每 `100ms` 以内持续发送结果到 STM32

STM32 侧的动作不是直接改 PWM，而是只改高层目标：

- `Target_Speed`
- `Turn_Speed`

然后仍然复用原三环控制输出电机 PWM。这个做法的好处是：

- 不破坏原平衡控制闭环
- 视觉策略只改行为层，不直接干预底层稳定性
- 新策略更容易继续扩展

### 17.3 K230 视觉避障策略细节

`control.c` 中当前策略为：

- 只接受 `color_id=1~3` 的红绿蓝目标
- 目标面积必须不小于 `200`
- 目标在左侧：小车右转绕行
- 目标在右侧：小车左转绕行
- 目标在中间：先进一步减速，再优先右转

对应参数：

- `K230_AVOID_BASE_SPEED = 8`
- `K230_AVOID_TURN_STRONG = 45`
- `K230_AVOID_TURN_MID = 30`
- `K230_AVOID_MIN_SIZE = 200`

### 17.4 视觉结果超时失效机制

`usart3.c` 中增加了视觉保活计数器：

- 每次收到合法颜色帧，就把计数器重装为 `40`
- 每次进入 `EXTI9_5_IRQHandler()`，都会调用 `K230_ColorFrameHeartbeat()`
- 如果连续多个控制周期没有新帧，当前视觉目标会自动清空

这个机制很重要，因为它避免了“最后一次识别结果永久生效”的问题。

### 17.5 当前 `README.md` 与实际代码不完全一致

仓库中的 `README.md` 仍然把 `USART3` 描述为“蓝牙串口”，并且没有完整说明 K230 视觉接入后的行为优先级。

因此，如果以后继续维护，建议以源码行为和本分析报告为准，不要只依赖 `README.md` 的概述。.

---

## 18. 启动后前后摇摆的调参建议（基于当前源码）

如果小车上电后能够勉强站住，但会持续出现“前后反复点头、来回摆动”，当前工程里优先检查和修改的参数都在：

`HARDWARE/CONTROL/control.c`

当前关键参数为：

- `Med_Angle = 0`
- `Vertical_Kp = -430`
- `Vertical_Kd = -1.92f`
- `Velocity_Kp = -0.44f`
- `Velocity_Ki = -0.0022f`

其中，启动后的前后摇摆，优先级通常是：

1. 先调 `Med_Angle`
2. 再调 `Vertical_Kp`
3. 再调 `Vertical_Kd`
4. 最后再碰 `Velocity_Kp / Velocity_Ki`

### 18.1 先看根因：前后摇摆通常不只是一个参数问题

当前控制链路是：

- `Velocity()` 先根据编码器速度输出一个“目标倾角补偿”
- `Vertical()` 再根据 `Pitch` 与 `Med_Angle + Velocity_out` 计算最终平衡 PWM

对应源码：

```c
Velocity_out = Velocity(Target_Speed, Encoder_Left, Encoder_Right);
Vertical_out = Vertical(Velocity_out + Med_Angle, Pitch, gyroy);
```

所以前后摇摆常见有三类原因：

- 平衡机械中值不对，车本来就没站在真正平衡点上
- 直立环比例或阻尼不合适，导致拉回过猛或阻尼不足
- 速度环积分过强，持续给直立环施加前后倾补偿

### 18.2 第一优先级：调 `Med_Angle`

参数位置：

```c
float Med_Angle = 0;
```

作用：

- 它不是 PID 增益，而是“机械平衡点修正值”
- 如果这个值不准，车会一直以为自己偏前或偏后，从而不断来回纠正

现象与修改方向：

- 如果小车一上电就明显偏向前冲，再被拉回来，说明平衡点可能偏了
- 如果小车静态时总想往前找平衡点，就微调 `Med_Angle`
- 如果小车静态时总想往后找平衡点，也微调 `Med_Angle`

推荐修改方法：

- 每次只改 `0.5 ~ 1.0`
- 例如先试：`0 -> 0.5 -> 1.0`，或者 `0 -> -0.5 -> -1.0`
- 哪个方向改完以后“更容易站住、前后摆幅更小”，就继续沿那个方向微调

建议：

- 不要一开始就大改成 `5`、`10`
- `Med_Angle` 先调到“小车可以比较安静地站住”，再调 PID

### 18.3 第二优先级：调直立环 `Vertical_Kp`

参数位置：

```c
float Vertical_Kp = -430;
```

作用：

- 决定角度偏差出现时，电机把车体拉回来的力度

现象与修改方向：

- 如果小车前后摆动幅度大、反应软、快倒了才猛拉，通常说明 `|Vertical_Kp|` 偏小
- 如果小车一有一点角度误差就猛烈抽动，前后快速来回打，通常说明 `|Vertical_Kp|` 偏大

修改原则：

- 当前是负值，调参时主要看“绝对值”
- 想增强拉回力度：从 `-430` 改到 `-440`、`-450`
- 想减弱过猛反应：从 `-430` 改到 `-420`、`-410`

推荐步进：

- 每次改 `5 ~ 15`
- 建议先小步试，不要一次跨太大

### 18.4 第三优先级：调直立环 `Vertical_Kd`

参数位置：

```c
float Vertical_Kd = -1.92f;
```

作用：

- 这是抑制前后摇摆的关键阻尼项
- 它直接利用 `gyro_Y` 的角速度来压制过冲

现象与修改方向：

- 如果小车能站住，但会连续前后“弹簧一样”来回晃，通常说明阻尼不够，应该适当增大 `|Vertical_Kd|`
- 如果小车动作发硬、发抖、像被强行刹住，甚至对噪声很敏感，通常说明 `|Vertical_Kd|` 偏大

修改原则：

- 想增强阻尼：`-1.92 -> -2.05 -> -2.15`
- 想减弱阻尼：`-1.92 -> -1.80 -> -1.70`

推荐步进：

- 每次改 `0.05 ~ 0.15`

对“启动后前后摇摆”这个现象来说：

- 一般先把 `Med_Angle` 调顺
- 再略增大 `|Vertical_Kd|`
- 再视情况微调 `|Vertical_Kp|`

这通常比先碰速度环更有效

### 18.5 第四优先级：调速度环 `Velocity_Kp`、`Velocity_Ki`

参数位置：

```c
float Velocity_Kp = -0.44f;
float Velocity_Ki = -0.0022f;
```

作用：

- 速度环并不是直接控电机
- 它会输出 `Velocity_out`，再作为直立环的目标倾角补偿

这意味着：

- 如果速度环太强，小车即使角度环已经基本稳定，还是会被不断推着前后修正
- 启动后那种“慢周期的大幅前后晃”，经常和速度环过强有关

现象与修改方向：

- 如果小车不是高频抖，而是慢慢前冲、慢慢后拉、周期比较长，先减小 `|Velocity_Kp|`
- 如果小车站住一会儿后，前后漂移越来越明显，可能是 `Velocity_Ki` 积分偏强

修改建议：

- `Velocity_Kp` 先从 `-0.44` 往 `-0.40`、`-0.36` 试
- `Velocity_Ki` 先从 `-0.0022` 往 `-0.0020`、`-0.0016` 试

推荐步进：

- `Velocity_Kp` 每次改 `0.02 ~ 0.05`
- `Velocity_Ki` 每次改 `0.0002 ~ 0.0005`

注意：

- 如果直立环本身还没调稳，不要急着大动速度环
- 速度环应该在“能稳住”的基础上再调成“不乱跑”

### 18.6 建议调参顺序

建议按下面顺序做，不要同时乱改多个参数：

1. 固定 `Velocity_Kp`、`Velocity_Ki`，先只调 `Med_Angle`
2. 在能基本站住后，微调 `Vertical_Kd`
3. 再调 `Vertical_Kp`
4. 最后再收尾调 `Velocity_Kp` 和 `Velocity_Ki`

建议每次只改一个参数，观察 2 到 3 次启动表现，再决定下一步。

### 18.7 针对“前后摇摆”的一套实操起点

如果当前现象就是“启动后前后摇摆”，可以直接按下面顺序试：

1. 保持 `Velocity_Kp=-0.44`、`Velocity_Ki=-0.0022` 不动
2. 先试 `Med_Angle = 0.5` 或 `-0.5`
3. 如果摆动还在，但已经更接近稳定，试 `Vertical_Kd = -2.05`
4. 如果还是偏软，再试 `Vertical_Kp = -440`
5. 如果变成反应太猛，就把 `Vertical_Kp` 拉回 `-425 ~ -430`

一个常见有效组合思路是：

- 先把 `Med_Angle` 调准
- 再略微增大 `|Vertical_Kd|`
- `Vertical_Kp` 只小步修正

### 18.8 其他常见情况与参数修改方向总结

#### 情况 1：能站住，但前后高频小抖

现象：

- 摆幅不大
- 但电机一直急促来回修正
- 车身像“发颤”

优先处理：

- 略减小 `|Vertical_Kp|`
- 或略减小 `|Vertical_Kd|`

建议：

- `Vertical_Kp: -430 -> -420`
- `Vertical_Kd: -1.92 -> -1.85`

#### 情况 2：能站住，但前后低频大摆

现象：

- 不是抖，而是慢慢前冲、慢慢后拉
- 一个周期比较长

优先处理：

- 先增大 `|Vertical_Kd|`
- 再减小 `|Velocity_Kp|`
- 必要时减小 `|Velocity_Ki|`

建议：

- `Vertical_Kd: -1.92 -> -2.05`
- `Velocity_Kp: -0.44 -> -0.40`
- `Velocity_Ki: -0.0022 -> -0.0018`

#### 情况 3：一启动就往前冲或往后冲

现象：

- 不是单纯摇摆，而是明显持续偏一个方向

优先处理：

- 先调 `Med_Angle`
- 再检查 MPU6050 安装方向、Pitch 正负方向是否正确
- 再检查左右电机正反转是否和程序一致

说明：

- 这种情况很多时候不是 PID 大小问题，而是平衡点、传感器方向或电机方向问题

#### 情况 4：扶起来以后很软，容易倒

现象：

- 车体像“没劲”
- 人手轻轻一放就倒

优先处理：

- 增大 `|Vertical_Kp|`
- 必要时略增大 `|Vertical_Kd|`

建议：

- `Vertical_Kp: -430 -> -445`
- `Vertical_Kd: -1.92 -> -2.00`

#### 情况 5：扶起来以后反应过猛，抽动明显

现象：

- 拉回动作太暴力
- 很容易过冲

优先处理：

- 减小 `|Vertical_Kp|`
- 或先略增大 `|Vertical_Kd|` 抑制过冲

建议：

- `Vertical_Kp: -430 -> -420`
- `Vertical_Kd: -1.92 -> -2.00`

#### 情况 6：能平衡，但总慢慢往前跑或往后跑

现象：

- 看起来站住了
- 但总会缓慢漂移

优先处理：

- 先调 `Med_Angle`
- 再小幅减小 `|Velocity_Ki|`

建议：

- 先微调 `Med_Angle`
- 再试 `Velocity_Ki: -0.0022 -> -0.0018`

#### 情况 7：原地左右不稳，像蛇形

现象：

- 不是前后摆，而是左右扭

优先处理：

- 看 `Turn_Kd`
- 看左右电机输出一致性
- 看车架、轮胎、重心是否左右对称

建议：

- `Turn_Kd: 0.6 -> 0.7`
- 若主动转向过猛，再减小 `Turn_Kp`

### 18.9 除了参数，还要排查的非参数问题

如果参数怎么改都不理想，常见不是算法本身，而是硬件一致性问题：

- `MPU6050` 没有校准好，零漂大
- 传感器安装不牢，车一震动传感器也跟着晃
- 电池电压不足，导致同样 PWM 下扭矩不稳定
- 左右电机性能差异过大
- 轮胎打滑，编码器速度反馈失真
- 车体重心过高或机械结构松动
- 齿轮箱间隙大，导致小幅控制时有死区

这些问题的表现通常是：

- 参数在一次测试里有效，换一次电池或换个地面又不行
- 左右轮输出看起来一样，但小车行为明显不一致

### 18.10 结论

对当前这份工程来说，启动后前后摇摆，最应该先动的不是全部 PID，而是：

1. `Med_Angle`
2. `Vertical_Kd`
3. `Vertical_Kp`
4. `Velocity_Kp / Velocity_Ki`

最稳妥的策略是：

- 先把机械平衡点调准
- 再用 `Vertical_Kd` 加阻尼压摆动
- 再用 `Vertical_Kp` 调拉回力度
- 最后才用速度环处理“慢漂”和“慢摆”



